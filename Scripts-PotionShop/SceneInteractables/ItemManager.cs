using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.VFX;

public class ItemManager : MonoBehaviour
{
   /* public Items[] items;

    public SpawnPoints[] ItemSpawnLocation;


    // Start is called before the first frame update
    void Start()
    {
        Items item = items[0];
        SpawnPoints spawn = ItemSpawnLocation[0];
        
        ItemSpawn(item.itemObj, spawn.SpawnPoint);
    }

    // Update is called once per frame
    void Update()
    {
       
    }

    
    
    void ItemSpawn(GameObject itemObj, GameObject SpawnPoint)
    {
        if (SpawnPoint != null)
        {
            Instantiate(itemObj, SpawnPoint.transform.position, SpawnPoint.transform.rotation);
        }     
    }

    void ItemSpawn2(GameObject itemObj, GameObject SpawnPoint)
    {
        Instantiate(itemObj, SpawnPoint.transform.position, SpawnPoint.transform.rotation);
    } */
}
